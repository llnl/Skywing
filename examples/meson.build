subdir('hello_world')
subdir('admm')
subdir('asynchronous_iterative')

if build_lc_examples
   subdir('lc_hello_world')
endif

subdir('push_sum')
# subdir('push_sum_ps')
subdir('sync_jacobi')
subdir('async_jacobi')
subdir('push_sum')
subdir('csv_example')

if use_helics
  subdir('helics_hello_world')
endif
