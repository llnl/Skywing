subdir('hello_world')
subdir('admm')
subdir('asynchronous_iterative')

if build_lc_examples
   subdir('lc_hello_world')
endif

subdir('sync_jacobi')
subdir('async_jacobi')
subdir('csv_example')

if use_helics
  subdir('helics_hello_world')
endif
